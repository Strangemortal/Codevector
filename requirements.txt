# requirements.txt
# ----------------
# Lists python package dependencies for the Fast Catalog application.
# Includes FastAPI, Uvicorn, the Supabase Client SDK, and python-dotenv.

fastapi>=0.100.0
uvicorn[standard]>=0.22.0
supabase>=2.0.0
python-dotenv>=1.0.0
